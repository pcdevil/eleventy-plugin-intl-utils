# ⚠️⚠️⚠️ PROJECT MOVED ⚠️⚠️⚠️

The project has been moved to Codeberg: <https://codeberg.org/pcdevil/eleventy-plugin-intl-utils>.

Execute the following commands to update your local copy to the new destination:

```zsh
$ cd eleventy-plugin-intl-utils
$ git remote set-url origin https://codeberg.org/pcdevil/eleventy-plugin-intl-utils
$ git fetch
$ git reset --hard @{u}
```
